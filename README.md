# Email Extractor - Улучшенная версия

## 📋 Описание

Email Extractor - это профессиональный инструмент для извлечения email-адресов с веб-страниц. Программа поддерживает обработку как отдельных URL, так и массовую обработку по категориям, с сохранением результатов в базу данных SQLite.

## ✨ Новые возможности

- 🗄️ **База данных SQLite** - централизованное хранение всех данных
- 📊 **Детальная статистика** - отслеживание прогресса и результатов
- 🔧 **Конфигурационные файлы** - гибкая настройка через YAML
- 📁 **Организованная структура** - логичное разделение файлов
- 🔄 **Обработка ошибок** - надежная работа с повторными попытками
- 📝 **Подробное логирование** - отслеживание всех операций
- 🎯 **Валидация email** - проверка корректности адресов
- 📈 **Прогресс-бар** - визуальное отображение процесса

## 🏗️ Структура проекта

```
Sales/
├── src/                          # Исходный код
│   ├── __init__.py
│   ├── email_extractor.py        # Основной модуль
│   ├── database.py               # Работа с БД
│   └── utils.py                  # Утилиты
├── data/                         # Данные
│   ├── input/urls/               # CSV файлы с URL
│   ├── output/emails/            # Результаты извлечения
│   ├── output/logs/              # Файлы логов
│   └── database/                 # База данных SQLite
├── config/                       # Конфигурация
│   ├── config.yaml              # Основные настройки
│   └── categories.yaml          # Категории товаров
├── tests/                        # Тесты
│   ├── __init__.py
│   ├── test_extractor.py        # Тесты основного модуля
│   └── test.py                  # Старый тестовый файл
├── requirements.txt              # Зависимости
├── pyproject.toml               # Конфигурация проекта
└── README.md                    # Документация
```

## 🚀 Установка и настройка

### 1. Установка зависимостей

```bash
# Установка через uv (рекомендуется)
uv pip install -r requirements.txt

# Или через pip
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Отредактируйте файл `config/config.yaml`:

```yaml
extraction:
  delay_between_requests: 30  # Задержка между запросами
  max_retries: 3              # Количество повторных попыток
  timeout: 20                 # Таймаут запроса
  use_proxies: false          # Использовать прокси

database:
  path: "data/database/emails.db"
  backup_enabled: true

logging:
  level: "INFO"
  file: "data/output/logs/email_extractor.log"
```

## 📖 Использование

### Обработка одной страницы

```bash
uv run python run.py example.com
```

### Обработка файла с URL

```bash
uv run python run.py -f data/input/urls/monitor.csv -o results.txt
```

### Обработка конкретной категории

```bash
uv run python run.py --category monitor
```

### Обработка всех категорий

```bash
uv run python run.py --all-categories
```

### Просмотр статистики

```bash
uv run python run.py --stats
```

## 🗄️ Работа с базой данных

### Просмотр всех email-адресов

```python
from src.database import EmailDatabase

db = EmailDatabase()
emails = db.get_all_emails()
for email_info in emails:
    print(f"{email_info['email']} - {email_info['company_name']}")
```

### Статистика по категориям

```python
stats = db.get_extraction_stats()
for stat in stats:
    print(f"Категория: {stat['category']}")
    print(f"URL: {stat['total_urls']}, Email: {stat['total_emails_found']}")
```

## 📊 Мониторинг и логирование

### Логи

Логи сохраняются в `data/output/logs/email_extractor.log` и содержат:
- Информацию о загруженных страницах
- Найденные email-адреса
- Ошибки и предупреждения
- Статистику обработки

### Статистика

Программа автоматически сохраняет статистику:
- Количество обработанных URL
- Количество найденных email-адресов
- Время выполнения
- Успешность извлечения по категориям

## 🔧 Конфигурация категорий

В файле `config/categories.yaml` настройте категории:

```yaml
categories:
  monitor:
    name: "Мониторы и дисплеи"
    keywords: ["монитор", "дисплей", "экран"]
    urls_file: "monitor.csv"
```

## 🧪 Тестирование

Запуск тестов:

```bash
# Все тесты
pytest tests/

# Конкретный тест
pytest tests/test_extractor.py::TestEmailValidation::test_valid_emails

# С покрытием кода
pytest --cov=src tests/
```

## 📈 Производительность

### Рекомендации по настройке

1. **Задержка между запросами**: 30-60 секунд для избежания блокировок
2. **Таймаут**: 20-30 секунд для медленных сайтов
3. **Повторные попытки**: 3-5 попыток для надежности
4. **Прокси**: Используйте для обхода ограничений

### Оптимизация

- Используйте SSD для базы данных
- Настройте регулярную очистку старых данных
- Создавайте резервные копии базы данных

## 🛠️ Разработка

### Структура кода

- `src/email_extractor.py` - основной класс EmailExtractor
- `src/database.py` - работа с базой данных
- `src/utils.py` - вспомогательные функции

### Добавление новых функций

1. Создайте тесты для новой функциональности
2. Реализуйте функцию в соответствующем модуле
3. Обновите документацию
4. Запустите тесты

## 📝 Логирование

### Уровни логирования

- `DEBUG` - подробная отладочная информация
- `INFO` - общая информация о работе
- `WARNING` - предупреждения
- `ERROR` - ошибки

### Настройка логирования

```python
from src.utils import setup_logging

logger = setup_logging("DEBUG", "custom.log")
```

## 🔒 Безопасность

### Рекомендации

1. Не используйте реальные email-адреса в тестах
2. Регулярно очищайте логи
3. Создавайте резервные копии базы данных
4. Используйте прокси для анонимности

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в `data/output/logs/`
2. Убедитесь в корректности конфигурации
3. Проверьте доступность интернет-соединения
4. Запустите тесты для диагностики

## 🎯 Планы развития

- [ ] Веб-интерфейс для управления
- [ ] API для интеграции с другими системами
- [ ] Поддержка дополнительных форматов экспорта
- [ ] Машинное обучение для улучшения извлечения
- [ ] Интеграция с CRM системами

## 📄 Лицензия

MIT License - см. файл LICENSE для подробностей.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Создайте Pull Request

---

**Версия**: 1.0.0  
**Автор**: Admin  
**Дата**: 2024